<%= form_with(model: canteen) do |form| %>
  <% if canteen.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(canteen.errors.count, "error") %> prohibited this canteen from being saved:</h2>

      <ul>
        <% canteen.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :canteen_name, style: "display: block" %>
    <%= form.text_field :canteen_name %>
  </div>

  <div>
    <%= form.label :password, style: "display: block" %>
    <%= form.text_field :password %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
